FC = ifort
FFLAGS = -g -traceback -I${GEMPAK}/include -I${OS_INC}
LIBS = ${BUFR_LIB4} \
       ${GEMLIB}/gemlib.a \
       ${GEMLIB}/appl.a \
       ${GEMLIB}/syslib.a \
       ${GEMLIB}/cgemlib.a \
       ${GEMLIB}/bridge.a \
       -L${gfrortran_ROOT}/lib64 -lgfortran
